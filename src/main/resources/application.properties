spring.application.name=BookInnApplication

#DB Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/bookinn
spring.datasource.username=USERNAME
spring.datasource.password=PASSWORD
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.servlet.context-path=/api/v1

jwt.secretKey=EnterJwtSecretKey

frontendUrl=http://localhost:8080

stripe.secret.key=EnterStripeSecretKey
stripe.webhook.secret=EnterWebhookSecretKey